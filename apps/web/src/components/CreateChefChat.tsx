"use client";

import { DashboardChefChat } from "@/components/DashboardChefChat";

/** Create tab chat — cues + suggestion saving. */
export function CreateChefChat() {
  return <DashboardChefChat context="create" showCues />;
}
