/** Max stored chat sessions per dashboard context (inventory, business, create). */
export const MAX_CHAT_SESSIONS = 5;

/** Max files attached per chat message (Sous Chef composer). */
export const MAX_CHAT_ATTACHMENTS = 5;

export const CHAT_ATTACHMENT_ACCEPT =
  ".png,.jpg,.jpeg,.pdf,image/*,application/pdf";
