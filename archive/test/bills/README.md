# Test bills (archived copy)

Bill fixtures and the generator live in the active test tree:

- **Generator:** [`test/scripts/generate-bills.py`](../../test/scripts/generate-bills.py)
- **Output:** [`test/bills/`](../../test/bills/)

```bash
python3 -m pip install -r test/scripts/requirements.txt
python3 test/scripts/generate-bills.py
```

This folder may contain stale copies from before the move — use `test/bills/` instead.
